# EG뮤세움 작업일지
> 2026.04.22 · 소로(기획·결정) × 파이스(P4 앱개발·운영)  
> 담당 파일: `museum.html`

---

## 오늘의 작업 요약

| # | 작업 항목 | 상태 |
|---|---|---|
| 1 | 3D 책 표지 디자인 탐색 및 목업 제작 | ✅ 완료 |
| 2 | 5종 표지 변주 + 마우스 틸팅 구현 | ✅ 완료 |
| 3 | 무한 랜덤 셔플 표지 생성기 구현 | ✅ 완료 |
| 4 | museum.html — 3D 표지 모달 이식 | ✅ 완료 |
| 5 | 페이지 전환 — 애플 북스 방식으로 완전 재작성 | ✅ 완료 |
| 6 | 이전/다음 버튼 제거 → 좌우 탭 영역으로 교체 | ✅ 완료 |
| 7 | 터치 드래그 실시간 추적 구현 | ✅ 완료 |
| 8 | 표지→내용 줌인 전환 애니메이션 구현 | ✅ 완료 |
| 9 | 필레몬 서신 페이지 제거 | ✅ 완료 |
| 10 | 마지막 페이지 완독 메시지 추가 | ✅ 완료 |

---

## 1. 3D 책 표지 디자인 — 탐색 과정

### 배경
기존 museum.html의 책등 클릭 시 뜨는 표지는 **단색 고동색 플랫 카드**였음.  
콘스텔라티오 별 카드 3D CSS 기법을 뮤세움 책 표지에 적용하는 방향으로 탐색 시작.

### 목업 제작 순서

**1차 목업** — 30종 스킨 샘플 (배경이 어두워 책이 묻히는 문제 발생)

**2차 목업** — 1권 집중 (시녀들 · Las Meninas)
- 밝은 크림 아이보리 배경으로 변경
- 홀로그램 광택 레이어 추가 (`conic-gradient` EG 스티커)
- 마우스 호버 시 스펙트럼 shimmer 효과
- 책등 72px 두께로 입체감 구현

**3차 목업** — 5종 타이포 변주
- Ver.1 인디고 아카데믹 (Noto Serif KR 울트라볼드)
- Ver.2 테라코타 빈티지 (Libre Baskerville)
- Ver.3 모노크롬 모던 (Noto Sans KR Black)
- Ver.4 에메랄드 고전 (고운바탕)
- Ver.5 골드 럭셔리 (Cormorant Garamond 라이트)

**4차 목업** — 피드백 반영 수정
- 책 위아래 삐죽 튀어나오는 `face-top/bottom` 제거
- 책 두께 3배 (26px → 72px)
- 바닥 그림자 3배 두툼하게

**5차 목업** — 무한 생성기
- 10종 팔레트 × 6종 타이포 × 3종 레이아웃 = **180+ 조합**
- `🎲 랜덤 표지 3권 생성` 버튼
- `strHash(answer)` 기반 item_id 시드 고정 → 같은 책은 항상 같은 표지

---

## 2. 3D 책 표지 — 핵심 CSS 기술

```css
/* 씬 */
.scene { perspective: 900px; perspective-origin: 50% 45%; }

/* 책 3D */
.book-3d {
  transform-style: preserve-3d;
  transform: rotateY(-26deg) rotateX(4deg);
  will-change: transform;
}

/* 앞표지 */
.face-front { border-radius: 0 5px 5px 0; backface-visibility: hidden; }

/* 책등 (72px 두께) */
.face-spine {
  width: 72px;
  left: -72px;
  transform-origin: right center;
  transform: rotateY(-90deg);
}

/* 홀로그램 레이어 */
.holo-layer {
  background: linear-gradient(118deg, transparent, rgba(255,80,140,0.07) ...);
  mix-blend-mode: screen;
}

/* EG 스티커 */
.cover-eg-sticker {
  background: conic-gradient(from 0deg, #ff6b9d, #c44dff, #4dc8ff ...);
}
```

### 마우스 틸팅
```javascript
scene.addEventListener('mousemove', e => {
  const dx = (e.clientX - cx) / (w/2);  // -1 ~ 1
  const dy = (e.clientY - cy) / (h/2);
  book.style.transform = `rotateY(${-26 + dx*14}deg) rotateX(${4 - dy*8}deg)`;
});
scene.addEventListener('mouseleave', () => {
  book.style.transform = 'rotateY(-26deg) rotateX(4deg)';
});
```

---

## 3. item_id 시드 기반 표지 고정 방식

```javascript
// 문자열 → 해시값
function strHash(s) {
  let h = 0;
  for (let c of s) h = (h * 31 + c.charCodeAt(0)) & 0xfffff;
  return h;
}

// 책 제목(answer)으로 팔레트·타이포·레이아웃 고정
const h    = strHash(book.answer);
const pal  = COVER_PALETTES[h % 10];          // 팔레트
const typo = COVER_TYPOS[(h >> 4) % 5];       // 타이포
const lay  = COVER_LAYOUTS[(h >> 8) % 3];     // 레이아웃
```

**효과**: 같은 책은 어느 기기·어느 세션에서 열어도 항상 동일한 표지. 무작위지만 일관성 유지.

---

## 4. 표지 → 내부 전환 애니메이션

### 흐름
```
책등 클릭
  → 3D 표지 모달 등장 (어두운 갈색 배경)
  → 마우스 틸팅 가능
  → 표지 클릭
    ① 배경이 갈색 → 크림 톤으로 전환 (1.2s)
    ② 책이 정면으로 회전 (rotateY 0deg, 0.48s)
    ③ scale(3.8) 줌인 + fadeout (1.0s)
    ④ 내부 페이지 fadein (0.38s)
```

### 핵심 타이밍
```javascript
// ① 정면 회전
book3d.style.transform = 'rotateY(0deg) rotateX(0deg)';

// ② 줌인 (450ms 후)
setTimeout(() => {
  book3d.style.transition = 'transform 1.0s ..., opacity 0.55s ease 0.45s';
  book3d.style.transform  = 'rotateY(0deg) rotateX(0deg) scale(3.8)';
  book3d.style.opacity    = '0';
}, 450);

// ③ 내부 페이지 등장 (1100ms 후)
setTimeout(() => showInnerPages(), 1100);
```

---

## 5. 애플 북스 방식 페이지 전환

### 기존 방식의 문제점
| 문제 | 원인 |
|---|---|
| 다음 페이지: 툭툭 튐 | `opacity 0→1` + 이동 동시 처리, 0.28s 너무 빠름 |
| 이전 페이지: 깜빡임 | `slide-in-left/out-right` 클래스 충돌, display 전환 타이밍 오류 |
| 버튼 디자인 | 네모 버튼이 페이지 하단에 별도 공간 차지 |

### 새 방식: 레이어 구조
```
[다음 페이지]  ← z-index:1, 바닥에 고정 (움직이지 않음)
[현재 페이지]  ← z-index:2, 그 위에 얹혀서 왼쪽으로 슬라이드아웃
```

### 다음 페이지 (→)
```javascript
inPage.style.zIndex    = '1';    // 바닥 고정
inPage.style.transform = 'translateX(0)';
inPage.style.transition = 'none';

outPage.style.zIndex   = '2';   // 위에서 슬라이드아웃
requestAnimationFrame(() => {
  outPage.style.transition = 'transform 0.48s cubic-bezier(0.32,0.72,0,1)';
  outPage.style.transform  = 'translateX(-100%)';
});
```

### 이전 페이지 (←)
```javascript
outPage.style.zIndex   = '1';    // 현재 페이지 바닥으로
inPage.style.transform = 'translateX(100%)';  // 이전 페이지 오른쪽 대기
requestAnimationFrame(() => requestAnimationFrame(() => {
  inPage.style.transition = 'transform 0.48s cubic-bezier(0.32,0.72,0,1)';
  inPage.style.transform  = 'translateX(0)';  // 위로 슬라이드인
}));
```

> `cubic-bezier(0.32, 0.72, 0, 1)` — iOS 네이티브 페이지 전환과 동일한 easing

---

## 6. 터치 드래그 실시간 추적

```javascript
// touchmove — 손가락 따라 실시간 이동
addEventListener('touchmove', e => {
  const dx = e.touches[0].clientX - touchStartX;
  if (dx < 0 && currentPage < totalPages - 1) {
    dragPage.style.transform = `translateX(${dx}px)`;  // 실시간 반영
  }
});

// touchend — 60px 이상이면 넘김, 미만이면 스프링백
addEventListener('touchend', e => {
  if (dx < -60)     turnPage(1);   // 다음
  else if (dx > 60) turnPage(-1);  // 이전
  else {
    // 스프링백
    dragPage.style.transition = 'transform 0.32s cubic-bezier(0.23,1,0.32,1)';
    dragPage.style.transform  = 'translateX(0)';
  }
});
```

---

## 7. 버튼 → 좌우 탭 영역으로 교체

```
기존: [ ← 이전 ]  ● ● ● ●  [ 다음 → ]  (하단 별도 영역)

신규: 페이지 위에 투명 좌/우 영역 (width: 44%)
      마우스 올리면 반투명 화살표 등장
      도트는 페이지 하단 중앙 오버레이
```

---

## 8. 필레몬 서신 페이지 제거

**배경**: brainstorm_20260418 에서 방향 전환 확정  
- 기존: 필레몬이 큐레이션 세팅 → 유저가 필레몬과 편지 교환  
- 신규: 집단지성 기고 시스템으로 대체

**변경**: `makePhilemonPage()` 함수를 필레몬 서신 없이 **완독 메시지 페이지**로 교체

```
        ✦
    [책 제목]
    ————————
  마지막 페이지입니다
이 책을 끝까지 읽었습니다.
서재에 별이 하나 켜졌습니다. 🌟

    [ 서재로 돌아가기 ]
```

---

## 9. 향후 연동 예정 (기억 사항)

### 기고 시스템 (미구현 🔴)
유저들이 각 책에 텍스트·이미지·링크 기고 → Claude API 심사 → 채택 시 닉네임 등재

```
현재 페이지 구조 (완성)
  3D표지 → 지식 → 사유 → 연결 → 큐레이션 → 완독메시지

기고 시스템 추가 후 (예정)
  3D표지 → 지식 → 사유 → 연결 → 큐레이션
         → [유저기고1 · 닉네임A · 날짜]
         → [유저기고2 · 이미지+텍스트 · 닉네임B]
         → ... (무한 확장)
         → 완독메시지
```

`buildPages()` 배열 구조가 이미 무한 확장에 대비되어 있으므로  
기고 페이지 추가는 Supabase `star_contents` 테이블 연동 후 push만 하면 됨.

### Supabase 연동 예정
```sql
-- 기고 페이지 조회 (status = 'published' 필터)
SELECT * FROM star_contents
WHERE answer = '시녀들'
AND status = 'published'
ORDER BY published_at ASC;
```

---

## 10. 생성 파일 목록

| 파일명 | 설명 |
|---|---|
| `museum_book_3d_sample.html` | 30종 스킨 첫 탐색 목업 |
| `museum_book_cover_mockup.html` | 1권 집중 목업 (시녀들) |
| `museum_book_5variants.html` | 5종 타이포 변주 목업 |
| `museum_book_infinite.html` | 무한 랜덤 셔플 생성기 |
| `museum_transition_demo.html` | 표지→내부 전환 데모 |
| `museum.html` | 최종 반영본 (GitHub 업로드용) |

---

## 메모

- **사그락 소리** (`page_turn.mp3`) 연동 유지됨 — `playPageSound()` 새 `turnPage()` 안에 살아있음
- **페이지 번호** 각 내지 우하단에 숫자 표시 유지됨
- **책 두께감** — `face-top/bottom` 제거, 책등 spine(72px)으로 두께 표현
- **item_id 시드** — `answer` 문자열 해시 기반, 기기/세션 무관하게 동일 표지 고정

---

*소로(기획·결정) × 파이스(P4 앱개발·운영) · 은교살롱 · 2026.04.22*  
*"책등 하나가 살아있는 교양이 된다."*
